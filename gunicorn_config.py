bind = '0.0.0.0:8000'  # Adjust the host and port as needed
workers = 4  # Adjust the number of workers based on your server's resources
module = 'app.py:app'  # Replace 'your_flask_app' with your Flask app's module name
